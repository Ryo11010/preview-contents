# SPA fallback for timetime preview
RewriteEngine On
RewriteBase /projects/timetime/stg/
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
RewriteRule ^ index.html [L]
