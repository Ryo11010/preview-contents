import { COLORS, radius, spacing } from './designTokens';

export const LOGIN_COLORS = {
  primary: COLORS.primary,
  background: '#FFFFFF',
  textMain: COLORS.textMain,
  textSub: COLORS.textSub,
  textTertiary: 'rgba(28,28,30,0.35)',
  border: COLORS.cardBorder,
  inputBg: '#FFFFFF',
  inputMuted: 'rgba(255,255,255,0.9)',
  danger: COLORS.danger,
  success: COLORS.success,
};

export const LOGIN_SPACING = {
  xs: spacing.xs,
  sm: spacing.sm,
  md: spacing.md,
  lg: spacing.lg,
  xl: spacing.xl,
  xxl: spacing.xl * 2,
};

export const LOGIN_RADIUS = {
  sm: radius.sm,
  md: radius.md,
  lg: radius.lg,
  xl: radius.xl,
  pill: radius.pill,
};

export const LOGIN_FONT_SIZES = {
  xs: 12,
  sm: 13,
  md: 16,
  lg: 22,
  xl: 28,
};

