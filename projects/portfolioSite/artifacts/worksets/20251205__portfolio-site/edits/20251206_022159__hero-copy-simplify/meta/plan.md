# Plan — Edit 20251206_022159__hero-copy-simplify

## 背景 / 目的
- Hero セクションのリード文をより簡潔にし、個人ポートフォリオとしてのメッセージを明瞭にする。
- 文言のみの変更で、コンポーネントやレイアウトには触れず、既存のデータドリブン構造を維持する。

## 参照 (Fail-Avoid / Success-Reuse)
- Failure: /artifacts に未登録（文言変更のみだがハードコード混入に注意）。
- Success:
  - 20251206_014702__funteru-content-refresh — data/config にコピーを集約した運用を踏襲。
  - 20251205_142847__app-debt-burndown — 型付きデータ辞書で UI を安定化、文言も data 側で管理した手法を再利用。
  - 20251204_214311__app-hardening — 文言/トークンの集中管理で可読性維持。

## agents.md 重要ポイント（抜粋）
- ハードコード絶対禁止：文言は data/config にのみ配置し、コンポーネント直書き禁止。
- A/B/C 提示、3+1 と file-map 更新必須。
- 500行ルール/命名規約順守（今回は文言のみ変更）。
- デバッグコンソール禁止（変更なし）。

## 代替案 (A/B/C)
- (A) `config/tokens.ts` の Hero コピーを短いフレーズに差し替える。UIは変更しない。  
  Pros: 最小変更・データ集中維持。Cons: インパクトが弱くなるリスク小。  
- (B) Hero コンポーネント側で動的に短縮文を分岐。  
  Cons: ロジック増・ハードコードリスク。  
- (C) セクション全体のテキストスタイル調整。  
  Cons: 範囲拡大・不要変更が増える。

### 採用案
- **(A)**：データファイルのみの文言差し替えで対応。

## 影響範囲
- `site/src/config/tokens.ts` の heroCopy のみ。UI/レイアウトは不変。

## テスト計画
- 手動: `npm run dev` で Hero 文言が簡潔に表示されることを確認（スタイル崩れなし）。
- 自動: Lint/型は未実施予定（文言のみ）。

## ハードコードに関する計画
- 文言は heroCopy に集約。コンポーネントへの直書きは行わない。

## ロールバック方針
- before/after を残すので heroCopy を元に戻せる。Git で差分リバートも可能。

## 相談・承認
- status: yes
- approver: user
- time: 2025-12-06T02:22:00Z
- memo: ヒーローテキスト簡潔化の依頼に基づき着手
