diff --git a/site/src/components/SectionHeader.tsx b/site/src/components/SectionHeader.tsx
index 4d6b6ec..284f1a0 100644
--- a/site/src/components/SectionHeader.tsx
+++ b/site/src/components/SectionHeader.tsx
@@ -1,15 +1,19 @@
 import React from 'react';
 import type { IconComponent } from '@/data/types';
 
-export const SectionHeader: React.FC<{ icon: IconComponent; title: string; align?: 'left' | 'center' }> = ({
-  icon: Icon,
-  title,
-  align = 'left',
-}) => (
+type SectionHeaderProps = {
+  icon?: IconComponent;
+  title: string;
+  align?: 'left' | 'center';
+};
+
+export const SectionHeader: React.FC<SectionHeaderProps> = ({ icon: Icon, title, align = 'left' }) => (
   <div className={`flex items-center gap-4 mb-16 pl-2 ${align === 'center' ? 'justify-center' : ''}`}>
-    <div className="p-3 bg-white/80 rounded-2xl shadow-sm border border-white/50 text-indigo-600">
-      <Icon size={28} />
-    </div>
+    {Icon ? (
+      <div className="p-3 bg-white/80 rounded-2xl shadow-sm border border-white/50 text-indigo-600">
+        <Icon size={28} />
+      </div>
+    ) : null}
     <h2 className="text-4xl font-bold text-slate-800 tracking-tight">{title}</h2>
   </div>
 );
diff --git a/site/src/components/sections/Timeline.tsx b/site/src/components/sections/Timeline.tsx
index f7d6fd2..fc1ffdc 100644
--- a/site/src/components/sections/Timeline.tsx
+++ b/site/src/components/sections/Timeline.tsx
@@ -1,5 +1,5 @@
 import React from 'react';
-import { ArrowUpRight, History } from 'lucide-react';
+import { ArrowUpRight } from 'lucide-react';
 import { SectionHeader } from '../SectionHeader';
 import type { TimelineSectionProps } from './types';
 import { LiquidCard } from '../LiquidCard';
@@ -7,7 +7,7 @@ import { getTimelineProject } from '@/data/timeline';
 
 export const TimelineSection: React.FC<TimelineSectionProps> = ({ items, onSelectProject }) => (
   <div id="history" className="max-w-4xl mx-auto px-6 py-24">
-    <SectionHeader icon={History} title="Journey" align="center" />
+    <SectionHeader title="Journey" align="center" />
 
     <div className="relative space-y-8">
       <div className="absolute left-[39px] top-8 bottom-8 w-0.5 bg-gradient-to-b from-transparent via-slate-300 to-transparent" />
diff --git a/site/src/components/sections/Work.tsx b/site/src/components/sections/Work.tsx
index 75027a8..b273851 100644
--- a/site/src/components/sections/Work.tsx
+++ b/site/src/components/sections/Work.tsx
@@ -1,12 +1,11 @@
 import React from 'react';
-import { Briefcase } from 'lucide-react';
 import { SectionHeader } from '../SectionHeader';
 import { FolderComponent } from '../Folder';
 import type { WorkSectionProps } from './types';
 
 export const WorkSection: React.FC<WorkSectionProps> = ({ categorizedProjects, categories, onSelectProject }) => (
   <div id="work" className="max-w-7xl mx-auto px-6 py-24 min-h-screen flex flex-col justify-center">
-    <SectionHeader icon={Briefcase} title="Work Archives — Ryo Tsurutani" align="center" />
+    <SectionHeader title="Work Archives — Ryo Tsurutani" align="center" />
 
     <div className="grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-8 justify-items-center">
       {categories.map((category, index) => {
